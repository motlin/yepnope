interface:
  display_name: "YepNope"
  short_description: "Try every binary question on the phone first"
  default_prompt: "Use $yepnope before every user-facing yes-or-no question and fall back natively only after trying it."
